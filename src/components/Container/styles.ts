import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(() => ({
    root: {
        padding: '40px'
    },
    cardContent: {
        marginTop: '30px'
    }
}));

export default styles;
